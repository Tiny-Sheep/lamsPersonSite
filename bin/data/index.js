'use strict'

const postData = require('./post-seed-data.json');
const userData = require('./user-seed-data.json');
const commentData = require('./comment-seed-data.json');

module.exports = {
  postData,
  userData,
  commentData,
};

